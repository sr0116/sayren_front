version: "3.8"

services:
  frontend:
    image: sr0116/sayren-frontend:latest   # ✅ Docker Hub에서 pull
    container_name: sayren_front
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    restart: always
